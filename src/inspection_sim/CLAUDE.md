# inspection_sim/CLAUDE.md

本包是历史 fake driver / 无硬件联调包。当前用户确认论文和演示不依赖 fake driver，因此本包不作为当前巡检主线。

## 当前定位

- 保留源码，避免破坏历史构建。
- 不作为论文系统架构、实验路线或验收条件。
- 不再按抓取方向新增 fake gripper / fake grasp perception 等节点。
- 若后续确实需要离线回放，优先做“图像/深度数据回放 + 算法验证脚本”，而不是恢复 fake 全系统。

## 主线替代方式

当前验证优先级：

1. 真机联调。
2. 半实物联调。
3. 离线数据回放算法验证。
4. 单元测试。

## 维护规则

- 不要在本包中实现真实算法。
- 不要把本包作为 `task_coordinator` 主流程依赖。
- 修改本包前先确认 `TODO.md` 中登记了明确用途。

